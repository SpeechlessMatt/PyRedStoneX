[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "redstonex"
version = "0.0.1"
dependencies = []

[tool.setuptools.packages.find]
include = ["redstonex*"]
